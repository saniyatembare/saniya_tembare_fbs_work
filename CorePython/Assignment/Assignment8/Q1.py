# Write a program to calculate area of rectangle 
def areaTriangle():
    b = float(input('Enter a base to print area of triangle : '))
    h = float(input('Enter a height to print area of triangle : '))
    area = (1/2)*b*h
    print(f'Area of the triangle is {area}')
areaTriangle()