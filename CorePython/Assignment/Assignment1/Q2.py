#Take length and breadth and calculate area of rectangle
length = float(input('Enter the lenght of triangle : '))
breadth = float(input('Enter the breadth of the rectangle : '))

#perform operation
Area = length * breadth

#display result
print(f'Area of reactangle is {Area}.')