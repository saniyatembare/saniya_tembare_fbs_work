#Take base and height of triangle and find its area
base = float(input('Enter base of the triangle : '))
height = float(input('Enter height of the triangle : '))

#perform operation 
Area = 1/2 * base * height

print(f'Area of triangle is {Area}')